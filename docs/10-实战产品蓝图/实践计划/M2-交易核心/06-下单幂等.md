# M2-06：下单幂等

返回 [实践计划与进度](../README.md)。

## 目标

同一用户携带同一幂等键的重复下单请求不重复创建订单。

## 范围

- 下单请求必填 `idempotencyKey`，长度为 1 至 64 个 ASCII 可见字符；为空、超长或含控制字符返回 400/40000。
- t_order 增加 `user_id + idempotency_key` 唯一索引，并持久化不可逆的 `request_fingerprint`。
- 请求指纹由已通过 M2-05 校验的下单语义生成：以当前用户 ID 和按 SKU ID 排序后的「SKU ID + 数量」列表构造规范化数据，再计算 SHA-256。不得直接哈希原始 JSON，以免字段顺序或数组顺序造成同义请求误判。
- 同一用户、同一幂等键、相同指纹：返回首次创建的订单，HTTP 200，且不重复预扣库存。
- 同一用户、同一幂等键、不同指纹：返回 HTTP 409 与 `code:40900`，不得静默重放另一份请求，也不得创建新订单。
- 创建订单的写事务与唯一键冲突后的回查必须分开：写入尝试在可独立回滚的事务中完成；发生唯一键冲突后，在该失败事务之外重新读取首单并比较指纹。禁止在已标记回滚的事务内捕获异常后继续回查或返回成功。

## 接口与旧数据约定

- 当前 `POST /orders` 请求示例为 `{"idempotencyKey":"checkout-001","items":[{"skuId":1,"quantity":2}]}`。键以当前 JWT 用户为作用域，精确区分字符，不修剪；允许 `!` 到 `~` 的 ASCII 可见字符，空格、控制字符和非 ASCII 均不允许。
- 先检查键及 M2-05 的明细结构（1–50 行、SKU 正整数不重复、数量 1–999），再将用户 ID 与按 SKU ID 排序的 `SKU ID:数量` 规范化数据计算 SHA-256。价格不属于请求语义；原单命中时不重新查询商品或库存，即使后续改价、下架或售罄也返回原单当前记录的信息。
- 旧学习订单在引入此能力前没有幂等键，迁移时两个新列均允许为空且保持原记录不变；新接口写入时两个字段均非空。数据库约束要求两个字段同为空或同为非空，`(user_id, idempotency_key)` 唯一约束兜底并发。已失败且回滚的下单不会占用键，库存补齐后可用同键重试。

## 学习点

幂等三板斧（token → 唯一索引 → 状态机条件更新）中的唯一索引方案、幂等键设计、并发幂等行为。

## 验收

1. 同键、同请求重复提交只产生一张订单；重放请求返回原单信息，库存预扣只发生一次。
2. 同键、不同 SKU 或数量的请求返回 409/40900，不返回首单作为成功结果。
3. 并发同键同请求仅创建一单，其余均拿到原单；并发同键不同请求仅允许其中一个创建，另一个返回 409/40900。
4. 不带幂等键、空键、超长键或非法字符的请求返回 400/40000。
5. `mvnw.cmd -pl services/user-service -am test` 在 JDK 25 下通过。

## 不做

- 不做预发 token 接口方案（三板斧第一斧，按成本需要再引入）。
- 不做支付/取消场景的幂等（天然由状态机条件更新承担）。

## 完成记录

日期：2026-09-17

提交：未提交（等待用户明确要求）。

测试与接口证据：在 `boot-server/` 执行 `mvnw.cmd -q -pl services/user-service -am test`，23 份 Surefire 报告共 101 个测试，失败、错误、跳过均为 0。本机 JDK 25 的 Unix domain loopback 异常仍需给测试进程临时挂载被忽略的 Java agent 回退 TCP，未修改仓库或用户级 Java 配置；`schema.sql` 重复执行测试通过。

- 真实 HTTP：同一用户同键同语义（含交换明细和 JSON 字段顺序）重放得到原订单，改价、下架和库存耗尽后也不新增订单或预扣；同键不同数量或 SKU 返回 `409/40900`，另一用户可使用同一键。并发同键同请求两个响应均成功且只留一单；并发同键不同请求恰一单成功、另一请求冲突，库存锁定量等于获胜订单数量。
- 失败和输入：缺货回滚后同键可在补库存后创建；缺失、空白、超长、非 ASCII 或控制字符键均返回 `400/40000`。领域测试验证指纹稳定性、用户和数量差异、键及重复 SKU 边界；原有订单事务、授权、OpenAPI 与非默认 MVC 前缀回归继续通过。
- 静态值审计：MVC 外部前缀仍由 `application.yml` 的 `spring.mvc.servlet.path` 提供，非默认前缀集成测试通过；错误分类由 `ErrorCode` 持有。幂等键与指纹由订单领域值对象定义，HTTP 字段名、DDL 列名与唯一约束作为协议和数据库映射就近声明；无新增部署配置或重复错误码。
