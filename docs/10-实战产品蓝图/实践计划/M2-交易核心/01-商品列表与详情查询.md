# M2-01：商品列表与详情查询

返回 [实践计划与进度](../README.md)。

## 目标

登录用户能分页浏览上架商品，并按 id 查看商品详情与 SKU 列表。

## 范围

- GET /products 分页（只返回上架商品，按 id 升序）。
- GET /products/{id} 详情聚合（商品信息 + SKU 列表 DTO）。
- 接口归类「仅认证」，无权限码。
- 响应 DTO 白名单，不暴露 deleted/version 等内部字段。

## 学习点

分页插件复用、多表聚合到 DTO、接口安全分类（公开 / 仅认证 / 权限保护）。

## 验收

1. 未登录访问返回 401/40100。
2. 登录后列表分页正确、详情含 SKU 与价格。
3. 下架商品不出现在列表，其详情返回 404/40400。
4. `./mvnw -pl services/user-service -am test` 通过。

## 不做

- 不做搜索与模糊查询。
- 不做缓存（M2.5）。
- 不做商品图片。

## 完成记录

日期：2026-09-15

提交：`feat: 新增商品列表与详情查询`。

测试与接口证据：

- `GET /products` 仅返回 `ON_SALE` 商品，按 ID 升序分页；`GET /products/{id}` 聚合未删除 SKU，草稿、下架或不存在商品统一返回 404/40400。两个接口只要求有效 JWT，无管理权限码，响应使用商品和 SKU DTO 白名单，未暴露 `deleted`、`version` 与审计字段。
- `ProductQueryIntegrationTests` 通过真实 HTTP Filter Chain 验证 4 个场景：无身份访问两个接口返回 401/40100；普通 USER 分页与详情成功且包含 SKU 精确价格；草稿和下架商品不可见、详情返回 404/40400；非法页码返回 400/40000。
- JDK 25.0.4.1 下执行 `boot-server/mvnw.cmd -pl services/user-service -am test`：60 项测试，0 失败、0 错误，`BUILD SUCCESS`；因本机 Unix domain socket 故障，本次测试进程沿用 M2-00 完成记录中的临时 TCP 管道回退 agent。`git diff --check` 通过。
