# M2-02：商品管理与改价

返回 [实践计划与进度](../README.md)。

## 目标

管理员能创建商品（含 SKU）并调整价格与上下架，并发改价冲突被拒绝。

## 范围

- 新增 product:manage 权限码并关联 ADMIN（schema 增量）。
- POST /products 创建商品与 SKU（请求 DTO 白名单，价格服务端校验非负）。
- PUT /skus/{id}/price 改价（乐观锁 @Version，version 不匹配返回 409/40900）。
- PUT /products/{id}/status 上下架。

## 学习点

权限码与 RBAC 关联、乐观锁冲突处理、写接口 DTO 白名单。

## 验收

1. 普通用户访问全部返回 403/40300，管理员成功。
2. 并发改价其一成功、另一返回 409/40900，不发生静默覆盖。
3. 上下架后 01 卡的 C 端可见性随之变化。
4. `./mvnw -pl services/user-service -am test` 通过。

## 不做

- 不做商品名称/描述编辑。
- 不做删除。
- 不做批量操作。
- 不做图片。

## 完成记录

日期：2026-09-15

提交：`feat: 新增商品管理与 SKU 乐观锁改价`。

测试与接口证据：

- `schema.sql` 按业务权限码幂等新增 `product:manage` 并关联 ADMIN；`SecurityConfig` 使商品 GET 仅需认证，商品创建、上下架和 SKU 改价必须具有该权限。商品与 SKU 由 `@Transactional` 同时创建，业务编码冲突时整笔回滚；改价由 `@Version` 与 MyBatis-Plus 乐观锁插件形成数据库版本条件更新，受影响行数为 0 返回 409/40900。
- `ProductManagementIntegrationTests` 4 项真实 HTTP 测试覆盖：无身份访问三个管理写接口返回 401/40100，普通 USER 返回 403/40300，ADMIN 创建商品并上下架后 M2-01 可见性立即变化；非法价格/状态返回 400/40000，重复 SKU 编码不留下半成品；两次同版本并发改价恰有一次成功、一次 409，版本只增加 1，旧版本重试仍被拒绝。
- JDK 25.0.4.1 下执行 `boot-server/mvnw.cmd -pl services/user-service -am test`：64 项测试，0 失败、0 错误，`BUILD SUCCESS`。本机 Unix domain socket 连接故障仍需使用已记录的进程级 TCP 管道回退 agent；`git diff --check` 通过。
- 静态值审计：可部署参数仍从 `application.yml` 与环境变量绑定读取，本卡未复制或更改默认配置；稳定商品状态由 `Product`、权限机器码由 `PermissionCodes` 拥有。SQL DDL/种子业务码、Controller 路由注解、Security 的内部路径、DTO 校验提示和测试请求保留就近字面量，状态校验正则引用 `Product` 常量。已有 `ApiPrefixIntegrationTests` 以非默认 MVC 前缀验证外部地址可变性，完整回归通过。
- 接入限制：本卡创建结果与改价结果返回 SKU 版本供连续改价；已有 SKU 的当前版本尚无独立管理读取接口，后续 B 端接入前需补齐管理员获取版本的契约，本卡没有扩展额外读取端点。
